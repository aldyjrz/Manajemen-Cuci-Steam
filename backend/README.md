# Backend scaffold

Minimal scaffold implementing parts of the AGENT.md specification: auth, user model, and structure.

To run:

1. Copy `.env.example` to `.env` and fill DB credentials.
2. Install dependencies:

```
cd backend
npm install
```

3. Run dev server:

```
npm run dev
```
